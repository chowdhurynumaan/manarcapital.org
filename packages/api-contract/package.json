{
  "name": "@manarcapital/api-contract",
  "version": "0.1.0",
  "private": true,
  "description": "Shared tRPC API contract for Manar Capital platform",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@trpc/server": "^11.0.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@manarcapital/config": "workspace:*",
    "tsup": "^8.4.0",
    "typescript": "^5.7.2"
  }
}
